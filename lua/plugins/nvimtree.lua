
return {
  "nvim-tree/nvim-tree.lua",
  opts = {
    view = {
      width = 30,
      side = "left",
    },

    git = {
      enable = false,
    },

    diagnostics = {
      enable = false,
    },

    renderer = {
      highlight_git = false,
      highlight_opened_files = "none",

        },

   },
}
